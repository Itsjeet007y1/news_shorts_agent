# Google Cloud Storage Configuration
gcs.bucket-name=news-image-bucket

# Firestore Configuration
# Leave firestore.project-id empty on Cloud Run to use the runtime project from Application Default Credentials.
# For local development, you may set this explicitly if project auto-detection is unavailable.
firestore.project-id=news-shorts-agent
# Firestore Native Mode default database
firestore.database-id=news-shorts-agent

